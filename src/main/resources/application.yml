server:
  port: ${port:8080}
  contextPath: /app

spring:
  profiles:
    active: dev
  application:
    name: tourism
  mvc:
    view:
      prefix: /
      suffix: .html
  thymeleaf:
    enabled: true

app:
  author: "Анна Гончарова"